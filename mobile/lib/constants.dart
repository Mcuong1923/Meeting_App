import 'package:flutter/material.dart';

// New color palette from the "EduQuest" design
const kPrimaryColor = Color(0xFF2C2255); // Dark purple for main actions/buttons
const kPrimaryLightColor =
    Color(0xFFFFFFFF); // White for card backgrounds, text fields
const kAccentColor =
    Color(0xFF7B61FF); // A bright accent purple like the previous meeting card
const kScaffoldBackgroundColor =
    Color(0xFFF3F1FD); // Light lavender for main background
const kTextColor = Colors.black87; // Standard dark text color

const double defaultPadding = 16.0;
